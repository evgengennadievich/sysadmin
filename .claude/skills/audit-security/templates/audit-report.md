# Security Audit — YYYY-MM-DD

**Сервер:** user@host
**Scope:** all / host / docker / git / tls
**Метод:** прогон `scripts/security-audit.sh` + ручная перепроверка каждой находки.
**Сводка:** X PASS / Y WARN / Z FAIL / N UNKNOWN

_Плюс K справочных строк (INFO) — перечни, а не проверки, в зачёт не идут._

> **Если UNKNOWN > 0 — вынеси список сюда, в шапку.** UNKNOWN означает «проверка не
> выполнена» (нет прав, нет каталога, нет утилиты), а не «нарушений не найдено».
> Отсутствие FAIL при непройденных проверках ничего не доказывает.
>
> **Непроверенное (N):**
> - <проверка> — <почему не удалось>

> **Если sudo без пароля недоступен** — скажи это прямо: часть проверок уровня root
> (UFW, fail2ban, `ss -p`, `sshd -T`) не выполнена.

---

## Host

| Проверка | Статус | Детали |
|----------|--------|--------|
| UFW активен и default deny | PASS / WARN / FAIL / UNKNOWN | Status: active, Default: deny incoming |
| UFW: состав правил | INFO / UNKNOWN | ALLOW IN: 22, 80, 443, … — каждое правило сверь с инвентарём |
| SSH: вход по паролю | PASS / FAIL / UNKNOWN | no (источник: `sshd -T` — действующая конфигурация) |
| SSH: вход root | PASS / WARN / UNKNOWN | no / prohibit-password |
| SSH: интерактивная аутентификация | PASS / WARN | обходной путь для паролей мимо PasswordAuthentication |
| fail2ban (служба) | PASS / FAIL / UNKNOWN | `systemctl is-active` → active. Пустой ответ = UNKNOWN, а не «не запущен» |
| fail2ban: sshd jail | PASS / WARN / UNKNOWN | активен; в бане сейчас N, всего забанено M |
| unattended-upgrades | PASS / WARN | включён, только security, без авто-перезагрузки |
| Внешние слушающие порты | INFO / UNKNOWN | список портов; нестандартные — не дефект, но должны быть в инвентаре |
| sudo: правила NOPASSWD | PASS / WARN / UNKNOWN | `NOPASSWD: ALL` = компрометация ключа даёт мгновенный root |
| Группа docker | PASS / WARN / UNKNOWN | состав группы = список тех, кто имеет root без sudo. `getent` показывает только дополнительных членов |

## Docker

| Проверка | Статус | Детали |
|----------|--------|--------|
| daemon.json | PASS / WARN | без insecure-registries |
| Права `.env` | PASS / WARN / UNKNOWN | **проверено файлов: N** — если N=0, это UNKNOWN, а не PASS. PASS только при поиске под root: без sudo `find` молча пропускает чужие каталоги |
| Публикация портов контейнеров | PASS / WARN | наружу публикуется только то, что должно |

> **Группа `docker` = root.** Если пользователь в ней состоит, он может смонтировать
> корень системы в контейнер и получить полную власть без всякого sudo. Учитывай это,
> оценивая пользу от сужения sudo — иначе закроешь одну дверь, оставив открытой соседнюю.

## Git

| Проверка | Статус | Детали |
|----------|--------|--------|
| gitleaks scan (working tree + history) | PASS / FAIL / UNKNOWN | N коммитов просмотрено, утечек нет |
| `.gitignore`: правила для секретов | PASS / WARN / UNKNOWN | `.env`, `*.key`, `*.pem`, `secrets/` |

## TLS

| Домен | Статус | Детали |
|-------|--------|--------|
| example.com | PASS | 2026-08-15 (113 дней), Verify return code: 0 (ok) |
| beta.example.com | WARN | 2026-05-10 (15 дней) |
| vpn.example.com | UNKNOWN | 443 не отдаёт сертификат — возможно, порт занят не-TLS сервисом |

> Домен, где 443 занят VPN-инбаундом, а не веб-сервером, — **штатная** конфигурация,
> а не сбой сертификата.

## Сверка топологии с инвентарём

Расхождения между тем, что показал аудит, и тем, что записано в `inventory/`.
Если расхождений нет — так и напиши. **Не заявляй расхождение, не перечитав запись:**
проще придумать несоответствие, чем проверить (было 2026-07-29).

## Рекомендации

Порядок — по соотношению «риск / цена исправления», а не по тяжести статуса.

1. **[WARN/FAIL]** Что не так → чем это грозит на практике → команда исправления →
   маркер зоны (Yellow/Red) → что может сломаться от исправления.

> Не рекомендуй «ужесточить» там, где ужесточение ломает работающее, без явного
> предупреждения о цене. Security-фикс, обрушивший прод, — это не улучшение.

## Историческая динамика (если есть предыдущий аудит)

Сравнение с `inventory/audits/<previous-date>.md`:

- **Улучшилось:** что зазеленело.
- **Не изменилось:** ключевые PASS, оставшиеся WARN.
- **Ухудшилось:** новые WARN/FAIL и почему появились.
- **Перешло из UNKNOWN в проверенное** (или наоборот) — отдельной строкой: рост числа
  UNKNOWN между аудитами означает, что мы стали видеть меньше, а не что стало безопаснее.

## Дефекты самого инструмента

Если при ручной перепроверке нашлись ложные срабатывания или пропуски скрипта —
зафиксируй здесь и заведи задачу для `/dev`. Инструмент, которому доверяют вслепую,
опаснее отсутствия инструмента.

## Метаданные

- Время прогона: ~N сек.
- Запускал: `bash scripts/security-audit.sh --server ... --scope ... --env-paths ...`
- Следующий плановый аудит: `YYYY-MM-DD` (через 90 дней — квартально).
