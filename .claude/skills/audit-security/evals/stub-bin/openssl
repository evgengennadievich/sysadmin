#!/bin/sh
# Заглушка openssl. s_client не отдаёт сертификат (сценарий «443 не отвечает»),
# всё остальное — настоящему openssl: разбор даты должен работать по-честному.
case "${1:-}" in
    s_client) exit 1 ;;
    *) exec /usr/bin/openssl "$@" ;;
esac
