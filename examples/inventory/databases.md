# ПРИМЕР, не реальные данные

# Базы данных (контейнер `my-postgres`)

> Этот файл — пример документации БД. Замени содержимое на реальные данные твоего сервера.
> Правило «honest unknown»: если данные неизвестны — пиши `? уточнить`.

Все базы данных живут в одном контейнере `my-postgres`. Контейнер доступен изнутри сети `my-app-network`
по имени `my-postgres` (порт 5432), а с хоста — через `127.0.0.1:5432`.

## Сводная таблица

| БД | Владелец-роль | Основной потребитель | Размер | Бэкапится сейчас? |
|----|--------------|---------------------|--------|-------------------|
| my_app_db | my_app_user | my-app | ? уточнить | ? уточнить |
| my_app_test | my_app_user | my-app (тестовая среда) | ? уточнить | нет |

---

## my_app_db

| Поле | Значение |
|------|----------|
| Владелец-роль | `my_app_user` |
| Размер | ? уточнить — запусти: `docker exec my-postgres psql -U my_app_user -c "\l+"` |
| Основные потребители | my-app |
| Бэкапится | ? уточнить — критичный пробел, заполни после настройки бэкапов |
| Статус | активный |

Хранит: <опиши основные таблицы — пользователи, заказы, статьи и т.д.>

---

## my_app_test

| Поле | Значение |
|------|----------|
| Владелец-роль | `my_app_user` |
| Размер | ? уточнить |
| Основные потребители | my-app (тестовая среда, CI) |
| Бэкапится | нет — тестовые данные, не критично |
| Статус | активный |

Хранит: копию схемы my_app_db для тестов.

---

## Как получить актуальные данные

Команды для заполнения раздела «Размер»:

```bash
# Запусти на сервере:
docker exec my-postgres psql -U postgres -c "\l+"
```
