# ПРИМЕР, не реальные данные

# Сервисы сервера 192.168.1.100

> Этот файл — пример реестра контейнеров. Формат: 9+ полей на контейнер, правило «honest unknown»:
> если данные неизвестны — пиши `? уточнить`, не выдумывай.
>
> Заполни своими контейнерами, удали или добавь блоки по необходимости.

## Сводная таблица

| Контейнер | Роль | Статус | Порт (хост:контейнер) | Домен |
|-----------|------|--------|-----------------------|-------|
| my-app | Основное приложение | активный | 127.0.0.1:3000→3000 | example.com |
| my-postgres | База данных PostgreSQL | активный (healthy) | 127.0.0.1:5432→5432 | — |
| my-redis | Кэш Redis | активный | internal | — |
| my-nginx | Reverse proxy | активный | 0.0.0.0:80→80, 0.0.0.0:443→443 | — |

---

## my-app

| Поле | Значение |
|------|----------|
| Имя контейнера | my-app |
| Роль | Основное веб-приложение. <опиши, что делает> |
| Порт | 127.0.0.1:3000→3000 |
| Домен | example.com (через my-nginx) |
| Путь на хосте | `/opt/my-app/` (compose) |
| Деплой | ? уточнить |
| Использует БД | my_app_db (см. databases.md) |
| Использует redis | да — my-redis |
| Критичность | высокая |
| Кто заметит падение | пользователи сайта |

**depends_on:** my-postgres, my-redis

**Переменные окружения** (имена; значения в менеджере паролей):
- DATABASE_URL
- REDIS_URL
- SECRET_KEY
- ? уточнить полный список из .env файла

---

## my-postgres

| Поле | Значение |
|------|----------|
| Имя контейнера | my-postgres |
| Роль | База данных PostgreSQL для my-app |
| Порт | 127.0.0.1:5432→5432 |
| Домен | — (только внутренняя сеть) |
| Путь на хосте | named volume `my_postgres_data` |
| Деплой | вместе с my-app через docker compose |
| Использует БД | — (сам является БД) |
| Использует redis | нет |
| Критичность | высокая |
| Кто заметит падение | my-app немедленно |

**Переменные окружения** (имена; значения в менеджере паролей):
- POSTGRES_PASSWORD
- POSTGRES_USER
- POSTGRES_DB

---

## my-redis

| Поле | Значение |
|------|----------|
| Имя контейнера | my-redis |
| Роль | Кэш и очереди задач для my-app |
| Порт | internal (только внутренняя сеть) |
| Домен | — |
| Путь на хосте | named volume `my_redis_data` |
| Деплой | вместе с my-app через docker compose |
| Использует БД | нет |
| Использует redis | — (сам является Redis) |
| Критичность | средняя (деградация без падения) |
| Кто заметит падение | замедление my-app |

---

## my-nginx

| Поле | Значение |
|------|----------|
| Имя контейнера | my-nginx |
| Роль | Reverse proxy: TLS-терминация, маршрутизация трафика на my-app |
| Порт | 0.0.0.0:80→80, 0.0.0.0:443→443 |
| Домен | — (сам является точкой входа) |
| Путь на хосте | `/opt/nginx/conf.d/`, `/etc/letsencrypt/` |
| Деплой | ? уточнить |
| Использует БД | нет |
| Использует redis | нет |
| Критичность | высокая |
| Кто заметит падение | все пользователи немедленно |
